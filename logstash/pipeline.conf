input {
  file {
    path => "/var/log/nginx/access.log"
    start_position => "beginning"
    sincedb_path => "/dev/null"   # để đọc lại file mỗi lần restart
    codec => "json"
  }
}

filter {
  geoip {
    source => "remote_addr"
    target => "geoip"
  }
  useragent {
    source => "http_user_agent"
    target => "useragent"
  }
  mutate {
    convert => {
      "status" => "integer"
      "body_bytes_sent" => "integer"
    }
  }
}

output {
  # Thêm output này vào để debug
  stdout { codec => rubydebug }

  # Giữ nguyên output cũ
  elasticsearch {
    hosts => ["http://elasticsearch:9200"]
    index => "nginx-%{+YYYY.MM.dd}"
  }
}

